#include <cstdio>
#include <iostream>
#include <cmath>

using namespace std;

const double eps = 1e-6;

int main()
{
    
  int a = 3;
  if (fabs(sqrt(3) * sqrt(3) - 3) < eps) puts("相等");

    return 0;
}